 # Comando para Prisma
 -- migarciones
 npx prisma migrate dev --name nombre_de la_migra
    -- generar cliente
    npx prisma generate

Para generar una clave en la terminal:
   openssl rand -base64 32

Para trabajar con postgresql
sudo -u postgres psql      

Crear una Base de Datos
sudo -u postgres psql -c "CREATE DATABASE nombre
_base_de_datos OWNER admin; 